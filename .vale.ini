StylesPath = .vale/styles
MinAlertLevel = suggestion
Vocab = RaspiBolt

# Core options (must appear before [section] blocks)
IgnoredScopes = code, tt, math, script, style, comment, link

[*.qmd]
BasedOnStyles = Vale, RaspiBolt
TokenIgnores = (`[^`]+`), (\{\{< [^>]+ >\}\})
